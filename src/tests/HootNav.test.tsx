import {render, screen, cleanup} from '@testing-library/react'
import '@testing-library/jest-dom'
import HootNav from "../components/HootNav";

describe("HootNav Component Test", () => {
    test('There should be one NavBar rendered', () => {
      // to be added        
    });

    test("Links for (Home, Login, Sign Up) should exist and lead to correct pages", () => {
        // To be added
    });

    
});