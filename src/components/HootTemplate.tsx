import React from 'react';
import { Typography } from '@mui/material';

export default function HootTemplate () {
    return(
        <Typography>
            Template Page
        </Typography>
    )
}
