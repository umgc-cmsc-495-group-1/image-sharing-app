import {render, screen, cleanup} from '@testing-library/react'
import '@testing-library/jest-dom'
import HootHome from "../components/HootHome";

describe("HootHome Component Test", () => {
    test('There should be one NavBar rendered', () => {
      // to be added        
    });

    
    test("Should contain picture of Mr.Owl", () => {
       // to be added
    });

    test("Links for (Home, About, Users) should exist and lead to correct pages", () => {
        // To be added
    });

    test("Is able to be clicked/ allows for user interaction", () => {
        // To be added
    });
});